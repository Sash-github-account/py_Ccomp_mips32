int main() {
    int a = 0, b = 1, c = 2, d = 3, e = 4, f = 5;

    while (a < 10) { a = a + 1; while (b < 5) { b = b + 2; d = d - 1;

    while (c > 0) { c = c - 1;

    while (d < 10) { d = d + 1;

    e = e + f; if (e > 10) { f = f - 2; } else { f = f + 3; }

    while (e > 0) { e = e - 1; d = d + e;

    while (f < 15) { f = f + 1; e = e - 2; } } f = f * 2; } a = a - 3; } } e = a + b + c + d + f; }

    c = a * b - d;

    while (c < 20) { c = c + 1;

    while (d > 0) { d = d - 1;

    while (e < 25) { e = e + 2;

    while (f > 0) { f = f - 1; e = e - f; }

    f = a + b + c - e; } e = a - b + d; } d = a + c - b; }

    }